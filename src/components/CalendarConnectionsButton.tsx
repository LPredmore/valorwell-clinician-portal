
import React from 'react';
import CalendarConnectionsButton from '@/components/calendar/CalendarConnectionsButton';

// Re-export the component to fix import issues
export default CalendarConnectionsButton;
